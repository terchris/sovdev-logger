# Talk sessions

AI-to-AI communication per [TALK.md](../../TALK.md). Active session: `talk.md`;
archived sessions: `talk<N>.md`. This folder is inside the repo, so NO secrets
in messages — identifiers and URLs only.
